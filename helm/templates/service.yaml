apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.config.prefix }}-sv"
  namespace: "{{ .Values.config.namespace }}"
spec:
  selector:
    app: {{ .Values.config.prefix }}
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
